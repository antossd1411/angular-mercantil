import { TurnToFloatDirective } from './turn-to-float.directive';

xdescribe('TurnToFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnToFloatDirective();
    expect(directive).toBeTruthy();
  });
});
